{
  "name": "yt-livechat-scraper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required'",
    "playwright:install": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nanoid": "^5.0.7",
    "playwright": "^1.46.0"
  },
  "engines": {
    "node": ">=18.x",
    "yarn": ">=1.22.0"
  }
}